name: Build Android APK (Disabled)

on:
  workflow_dispatch:
  # 禁用此工作流，使用新的 build-android.yml

jobs:
  build:
    runs-on: ubuntu-latest
    if: false  # 禁用此工作流
    
    steps:
    - name: This workflow is disabled
      run: echo "This workflow has been disabled. Use build-android.yml instead."