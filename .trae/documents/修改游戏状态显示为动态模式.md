我需要修改场景编辑区域的布局，具体包括：

1. **缩小场景ID输入框**：将其宽度从原来的1fr改为1/3，因为它是只读的默认生成标识
2. **增大场景名称文本框**：将其宽度从原来的1fr改为2/3，使其占据更多空间
3. **调整删除按钮位置**：将删除按钮与场景名称文本框放在同一行，修复当前的错位问题

修改位置：`src/app/studio/page.tsx` 第637-675行

修改方案：

1. 将场景信息的grid布局从`grid grid-cols-2`改为`grid grid-cols-[1fr_3fr]`
2. 调整flex布局，使删除按钮与场景信息在同一行垂直对齐
3. 修改删除按钮的对齐方式，使其与输入框在同一水平线上

