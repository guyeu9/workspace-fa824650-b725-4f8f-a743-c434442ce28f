## 修复 game-library/page.tsx JSX 结构错误

### 问题分析
文件中有 JSX 结构问题，导致编译错误：
- 第753行有一个孤立的 `)}` 碎片
- 这导致后续代码被错误解析

### 修复方案

**1. 修复第753行的孤立 `)}`**
```typescript
// 当前（错误）：
          </div>
        )}
```

```typescript
// 修复后：
          </div>
        )}
```

**2. 确保正确的 JSX 结构**
- 底部操作栏（703行）应该放在条件渲染块内部
- 社区模式（viewMode === 'community'）有自己独立的结构
- 确保所有 div 正确嵌套

**3. 验证文件结构**
修复后文件应该能够正常编译。

### 执行时间
约 2 分钟